---
tc_signature: "+0k7hbiIAgrWOvfwsUYFfaZcHsGwMcfcDHSaNdzrphbku9b+Ki+jsguMVqOFIWU4pPh2JdEMCyXOGyapOwlaBw=="
title: "PetstoreClient_Integration"
description: "Runnable Python Client for the Swagger Petstore - OpenAPI 3.0 generated from OpenAPI spec."
tags:
  - petstore
  - openapi
  - integration
  - autogenerated
  - pydantic
author: "TrustChain Agent Synthesis"
---
# PetstoreClient_Integration

## Context
Autogenerated API client for Swagger Petstore - OpenAPI 3.0 (OpenAPI ingestion).

Base URL: https://petstore3.swagger.io/api/v3
Total endpoints parsed: 19
Pydantic schemas automatically generated via `datamodel-code-generator`.


## Solution
To interact with petstore, create an instance of `PetstoreClient` and call its endpoints.

The module includes full Pydantic models for request/response serialization.

```python
"""
Autogenerated Pydantic Models and HTTPX Client for Swagger Petstore - OpenAPI 3.0
"""

import httpx
from typing import Dict, Any, Optional, List, Union
from pydantic import BaseModel, Field, RootModel

# generated by datamodel-codegen:
#   filename:  tmpelyt9g5q.json
#   timestamp: 2026-02-21T17:16:54+00:00



from enum import Enum





class Status(Enum):
    available = 'available'
    pending = 'pending'
    sold = 'sold'


class StoreInventoryGetResponse(RootModel[Dict[str, int]]):
    root: Dict[str, int]


class ApiResponse(BaseModel):
    code: Optional[int] = None
    type: Optional[str] = None
    message: Optional[str] = None


class Status1(Enum):
    placed = 'placed'
    approved = 'approved'
    delivered = 'delivered'


class Order(BaseModel):
    id: Optional[int] = Field(None, examples=[10])
    petId: Optional[int] = Field(None, examples=[198772])
    quantity: Optional[int] = Field(None, examples=[7])
    shipDate: Optional[AwareDatetime] = None
    status: Optional[Status1] = Field(
        None, description='Order Status', examples=['approved']
    )
    complete: Optional[bool] = None


class Status2(Enum):
    available = 'available'
    pending = 'pending'
    sold = 'sold'


class User(BaseModel):
    id: Optional[int] = Field(None, examples=[10])
    username: Optional[str] = Field(None, examples=['theUser'])
    firstName: Optional[str] = Field(None, examples=['John'])
    lastName: Optional[str] = Field(None, examples=['James'])
    email: Optional[str] = Field(None, examples=['john@email.com'])
    password: Optional[str] = Field(None, examples=['12345'])
    phone: Optional[str] = Field(None, examples=['12345'])
    userStatus: Optional[int] = Field(None, description='User Status', examples=[1])


class Category(BaseModel):
    id: Optional[int] = Field(None, examples=[1])
    name: Optional[str] = Field(None, examples=['Dogs'])


class Tag(BaseModel):
    id: Optional[int] = None
    name: Optional[str] = None


class UserCreateWithListPostRequest(RootModel[List[User]]):
    root: List[User]


class Pet(BaseModel):
    id: Optional[int] = Field(None, examples=[10])
    name: str = Field(..., examples=['doggie'])
    category: Optional[Category] = None
    photoUrls: List[str]
    tags: Optional[List[Tag]] = None
    status: Optional[Status2] = Field(None, description='pet status in the store')


class PetFindByStatusGetResponse(RootModel[List[Pet]]):
    root: List[Pet]


class PetFindByTagsGetResponse(RootModel[List[Pet]]):
    root: List[Pet]

class PetstoreClient:
    """Autogenerated Client for Swagger Petstore - OpenAPI 3.0"""

    def __init__(self, api_key: str = "", base_url: str = "https://petstore3.swagger.io/api/v3"):
        self.base_url = base_url.rstrip("/")
        self.headers = {"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"} if api_key else {}

    def updatepet(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Update an existing pet.
        """
        url = self.base_url + "/pet"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="PUT",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def addpet(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Add a new pet to the store.
        """
        url = self.base_url + "/pet"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def findpetsbystatus(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Finds Pets by status.
        """
        url = self.base_url + "/pet/findByStatus"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def findpetsbytags(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Finds Pets by tags.
        """
        url = self.base_url + "/pet/findByTags"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def getpetbyid(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Find pet by ID.
        """
        url = self.base_url + "/pet/{petId}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def updatepetwithform(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Updates a pet in the store with form data.
        """
        url = self.base_url + "/pet/{petId}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def deletepet(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Deletes a pet.
        """
        url = self.base_url + "/pet/{petId}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="DELETE",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def uploadfile(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Uploads an image.
        """
        url = self.base_url + "/pet/{petId}/uploadImage"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def getinventory(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Returns pet inventories by status.
        """
        url = self.base_url + "/store/inventory"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def placeorder(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Place an order for a pet.
        """
        url = self.base_url + "/store/order"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def getorderbyid(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Find purchase order by ID.
        """
        url = self.base_url + "/store/order/{orderId}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def deleteorder(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Delete purchase order by identifier.
        """
        url = self.base_url + "/store/order/{orderId}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="DELETE",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def createuser(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Create user.
        """
        url = self.base_url + "/user"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def createuserswithlistinput(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Creates list of users with given input array.
        """
        url = self.base_url + "/user/createWithList"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="POST",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def loginuser(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Logs user into the system.
        """
        url = self.base_url + "/user/login"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def logoutuser(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Logs out current logged in user session.
        """
        url = self.base_url + "/user/logout"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def getuserbyname(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Get user by user name.
        """
        url = self.base_url + "/user/{username}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="GET",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def updateuser(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Update user resource.
        """
        url = self.base_url + "/user/{username}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="PUT",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )

    def deleteuser(self, path_params: Dict[str, Any] = None, data: Dict[str, Any] = None, params: Dict[str, Any] = None) -> httpx.Response:
        """
        Delete user resource.
        """
        url = self.base_url + "/user/{username}"
        if path_params:
            for k, v in path_params.items():
                url = url.replace("{k}", str(v))

        with httpx.Client() as client:
            return client.request(
                method="DELETE",
                url=url,
                json=data,
                params=params,
                headers=self.headers
            )


```

